# V2 Follow-Up To Source Code Review

## Refactors Completed

| Code review item | Status | Implementation |
|---|---|---|
| Validate invalid givens consistently | Done | `solve_recursive_backtracking`, custom heuristics, and Gurobi now call `validate_partial` before search/model build. |
| Avoid repeated unit scans in propagation | Done | `UNITS_BY_CELL` was added to `grid.py` and `_eliminate` now checks only relevant units. |
| Name solver stages clearly | Done | Added `heuristic_v1_constraint_mrv` and `heuristic_v2_adaptive_locked_sets`. |
| Add uniqueness-aware classification | Done | `classify_puzzle` reports `invalid`, `no_solution`, `unique`, or `multiple_solutions`. |
| Make Gurobi statuses readable | Done | Gurobi raw statuses are mapped to names such as `optimal` and `infeasible`; solved rows use `ok`. |
| Reuse difficulty-score solver stats | Done | `difficulty_score(grid, solved=...)` avoids an extra solve during generation. |
| Update benchmark comparison | Done | Benchmark now includes plain recursive, V1, V2, Gurobi default, and Gurobi heuristic mode. |

## Remaining Improvement Ideas

- Add a larger public puzzle corpus to reduce benchmark sampling risk.
- Add a stronger official difficulty reference if the project needs a formal rating system.
- Consider X-wing/chains only if the educational narrative can stay readable.
- Consider a Gurobi MIP-start experiment if the optimisation baseline needs deeper tuning.
